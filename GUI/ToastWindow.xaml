<Window x:Class="GUI.ToastWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="300" Height="100"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Loaded="Window_Loaded">

    <Border Background="#FF323232" CornerRadius="10" Padding="10" Opacity="0.9">
        <StackPanel>
            <TextBlock x:Name="TitleBlock" FontWeight="Bold" Foreground="White" FontSize="16"/>
            <TextBlock x:Name="MessageBlock" Foreground="White" TextWrapping="Wrap" Margin="0,5,0,0"/>
        </StackPanel>
    </Border>
</Window>